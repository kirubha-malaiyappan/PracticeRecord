using System;

class Program
{
    static void Main(string[] args)
    {
        // Strings
        StringExamples.Run();

        // Ref / In / Out
        RefExample.Run();
        InExample.Run();
        OutExample.Run();

        // OOP
        OopDemo.Run();
    }
}
